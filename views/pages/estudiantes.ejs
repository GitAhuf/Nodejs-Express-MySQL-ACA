<!DOCTYPE html>
<html lang="es">
  <head>
    <%- include('../includes/head') %>
  </head>
  <body>
    <%- include('../includes/header') %>

    <main class="content-wrapper mt-5" style="margin-top:80px!important">
      <div class="container">
        <!-- Título -->
        <div class="row justify-content-center mb-4">
          <div class="col-md-12 text-center">
            <h2>CRUD Estudiantes</h2>
            <hr />
          </div>
        </div>

        <div class="row">
          <!-- Formulario de registro -->
          <div class="col-md-4 mb-4">
            <h3>Registrar Estudiante</h3>
            <form action="/estudiantes" method="POST">
              <div class="mb-3">
                <label class="form-label">Nombre</label>
                <input
                  type="text"
                  name="nombre_alumno"
                  class="form-control"
                  required
                />
              </div>
              <div class="mb-3">
                <label class="form-label">Email</label>
                <input
                  type="email"
                  name="email_alumno"
                  class="form-control"
                  required
                />
              </div>
              <div class="mb-3">
                <label class="form-label">Curso</label>
                <select name="curso_alumno" class="form-select" required>
                  <option value="">-- Seleccionar --</option>
                  <option value="Python">Python</option>
                  <option value="NodeJS">NodeJS</option>
                  <option value="PHP">PHP</option>
                  <option value="JavaScript">JavaScript</option>
                </select>
              </div>
              <div class="d-grid">
                <button class="btn btn-primary">Registrar</button>
              </div>
            </form>
          </div>

          <!-- Tabla de estudiantes -->
          <div class="col-md-8">
            <h3>Listado de Estudiantes</h3>
            <div class="table-responsive">
              <table class="table table-striped">
                <thead class="table-dark">
                  <tr>
                    <th>#</th>
                    <th>Nombre</th>
                    <th>Email</th>
                    <th>Curso</th>
                    <th>Acciones</th>
                  </tr>
                </thead>
                <tbody>
                  <% estudiantes.forEach((e, i) => { %>
                    <tr>
                      <td><%= i + 1 %></td>
                      <td><%= e.nombre %></td>
                      <td><%= e.email %></td>
                      <td><%= e.curso %></td>
                      <td>
                        <a
                          href="/detalles/<%= e.id %>"
                          class="btn btn-info btn-sm"
                        >Ver</a>
                        <a
                          href="/formulario-actualizar-estudiante/<%= e.id %>"
                          class="btn btn-warning btn-sm"
                        >Editar</a>
                        <form
                          action="/borrar-estudiante/<%= e.id %>"
                          method="POST"
                          style="display:inline"
                        >
                          <button
                            type="submit"
                            class="btn btn-danger btn-sm"
                          >
                            Borrar
                          </button>
                        </form>
                      </td>
                    </tr>
                  <% }) %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </main>

    <%- include('../includes/scripts') %>
  </body>
</html>
